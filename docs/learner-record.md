<a id="your-bookcatalog-learner-record"></a>
# Optional BookCatalog learner record

Want a detailed workbook? Copy the sections you find useful into local notes. None of this record is required for course completion.

The six core steps are introduction, Setup, assessment, planning, execution, and Azure planning.

Use **Pass**, **Fail**, or **Not run** for checks, with actual observations and a short reason. Mark optional sections **Skipped** when appropriate.

Use sample data only. Keep credentials, tokens, database files, and local snapshots out of Git and chat.

## Baseline

- Date, branch, and baseline commit:
- Visual Studio version and selected .NET SDK:
- Legacy local URL:
- Upgrade scenario and assessment path:
- Unresolved setup or behavior results:

### Records to preserve

The core course rebuilds disposable demo data through EF Core. Existing records don't need to survive the upgrade.

If you want a separate import exercise, use the [standalone data-transfer reference](data-transfer.md).

### Behavior checks

This detailed checklist is optional. Use it on the legacy app or your upgraded app. Cloud checks require separate deployment approval.

For the required local check, add a book, edit it, restart the app, and confirm the saved edit remains.

The other checks below are optional. For the upgraded app, use new sample records in `BookCatalogModernizedLab`.

| Check and acceptable result | Legacy | Upgraded | Optional Azure |
| --- | --- | --- | --- |
| Main list includes active books in title order | | | |
| A new book's saved edit remains after application restart | | | |
| Empty title/author, title over 200 characters, and year outside 1800–2100 are rejected | | | |
| Throwaway edits persist without changing its stored CreatedDate | | | |
| Inactive throwaway record leaves the list, but its details route works | | | |
| Restoring throwaway active status puts it back in title order | | | |
| An unused ID returns HTTP 404, not an empty success page | | | |
| Deleting the throwaway record makes its details route return 404 | | | |
| A write without a valid antiforgery token is rejected | | | |

Seed counts apply only before additions. Use `/Books/Details/2147483647` for a missing ID only after confirming it is unused.

See [advanced checks](advanced-checks.md#check-behavior-with-a-separate-record) for server validation, antiforgery, and stored creation time.

## Assessment decisions

Review the actual assessment generated by the modernization agent. You can keep it as-is if it describes the required upgrade adequately.

If it needs context, edit the report or ask the agent through Copilot Chat. This table is for a deeper optional review.

| Finding and source location | Compatibility category | User impact | Fix, replace, remove, or defer | Check |
| --- | --- | --- | --- | --- |
| | | | | |

- Why I kept the assessment or chose to change it:
- Assessment section I edited, if any:
- Preference or context I gave the modernization agent:
- What the agent changed, if requested:
- Any disputed or unverified finding:

## Requirements and upgrade plan

Generate the plan first, then review its choices. Point to actual plan sections, not only chat replies.

Use an in-place ASP.NET Core MVC rewrite on .NET 10 with EF Core. Don't keep the old host, shared schema, YARP, or System.Web adapters.

| Plan choice or requirement | Plan section | Review or result |
| --- | --- | --- |
| Replace the existing web host with ASP.NET Core MVC on .NET 10 | | |
| Replace EF6 with EF Core | | |
| Rebuild and seed BookCatalogModernizedLab as needed, not on every startup | | |
| Add a book, edit it, restart the app, and confirm the saved edit remains | | |

- EF Core choice and reason:
- Optional alternative considered:
- Add/edit/restart requirement I added:
- Agent's response to the edit:
- Reviewed plan path and checkpoint:

## Selected-record comparison

This belongs only to the [standalone transfer exercise](data-transfer.md#preview-copy-and-verify-the-selected-records), not the core course.

If you choose that exercise, record its preview, apply, and verify results in your own notes.

## Upgrade review and recovery

- Representative diff I checked:
- Behavior results and unresolved failures:
- Last reviewed commit and last successful command:
- Existing scenario and next pending group after reopening:
- Optional [author-filter challenge](author-filter.md) and its checks:

## Azure assessment and plan

- Actual assessment/configuration and migration-plan paths:
- Relevant finding and proposed action:
- Hosting target chosen:
- Optional source investigation or alternative considered:
- Plan section I edited:
- What the agent changed after reconciliation:
- LocalDB replacement and EF Core schema/seed preparation for the cloud database:
- Configuration and runtime identity responsibilities:
- Developer/administrator responsibilities:
- Access, region, cost, and public-access assumptions:
- Deployment checks, diagnostics, recovery, and cleanup owner:
- Unresolved questions before any deployment:

Required course completion ends with a reviewed Azure plan. It doesn't require a subscription, deployed resources, or this workbook.

## Optional deployment evidence

Leave this section **Not run** if you stop after planning.

- Approved subscription, scope, region, budget, and owner:
- Reviewed outputs file and dedicated resource group:
- Schema and seed preparation result:
- New disposable cloud record and ordinary app/restart results:
- Optional advanced request and stored-timestamp checks:
- Temporary firewall-rule cleanup result:
- Dedicated resource-group deletion result:

## What I can explain

- A requirement that changed the plan:
- A build success that needed a separate behavior or data check:
- A decision I would change for a larger application:
- My next safe action if a check fails:
